<form method="post" action="{{ path('listephp_delete', {'idlistephp': listephp.idlistephp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listephp.idlistephp) }}">
    <button class="btn">Delete</button>
</form>
