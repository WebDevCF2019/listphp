{% extends 'base.html.twig' %}

{% block title %}Listephp{% endblock %}

{% block body %}
    <h1>Listephp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idlistephp</th>
                <td>{{ listephp.idlistephp }}</td>
            </tr>
            <tr>
                <th>Thetitle</th>
                <td>{{ listephp.thetitle }}</td>
            </tr>
            <tr>
                <th>Thedesc</th>
                <td>{{ listephp.thedesc }}</td>
            </tr>
            <tr>
                <th>Thetext</th>
                <td>{{ listephp.thetext }}</td>
            </tr>
            <tr>
                <th>Thedate</th>
                <td>{{ listephp.thedate ? listephp.thedate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('listephp_index') }}">back to list</a>

    <a href="{{ path('listephp_edit', {'idlistephp': listephp.idlistephp}) }}">edit</a>

    {{ include('listephp/_delete_form.html.twig') }}
{% endblock %}
